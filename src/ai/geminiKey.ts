/**
 * Cloud AI key helpers — OpenRouter (Gemini key APIs kept as aliases).
 */
export {
  getStoredOpenRouterApiKey as getStoredGeminiApiKey,
  setStoredOpenRouterApiKey as setStoredGeminiApiKey,
  resolveOpenRouterApiKey as resolveGeminiApiKey,
  hasOpenRouterApiKey as hasGeminiApiKey,
  getStoredOpenRouterApiKey,
  setStoredOpenRouterApiKey,
  resolveOpenRouterApiKey,
  hasOpenRouterApiKey,
} from './openrouter';
